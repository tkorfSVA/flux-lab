apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 5m
  path: "./infrastructure/namespaces"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client